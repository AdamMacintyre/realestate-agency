import React from 'react'

import './topbanner.scss'

const TopBanner = (props) => {
  return (
    <div className='top__banner' style={{ backgroundImage: `url(${props.backgroundImage})` }}>

      </div>
  )
}

export default TopBanner